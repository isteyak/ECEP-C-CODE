

// system call are  mediun or way to request services from  kernal

//core of operating system is kernal

//if service is required so it is requested from kernal
//fork() -> create a process
// it takes no argument
// it return process id of the child process its created
//after creation of child process both child and parent start execution from next instruction
//fork() > 0 -> successfull creation of parent process
//fork() < 0 -> this is in error case
//fork() = 0 -> this is child process


/*
   p1

   f() -> child process will be created c1
   f()
   p()




*/
/*
main()
{
    fork();
    fork();

    printf("Hello\n");
}


*/
