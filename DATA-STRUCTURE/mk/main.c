#include <stdio.h>

int sum(int, int);
int sub(int, int);
int mul(int, int);

int main()
{

    printf("%d is sum\n", sum(6, 5));
    printf("%d is sub\n", sum(9, 5));
    printf("%d is mul\n", sum(5, 5));

    return 0;
}
